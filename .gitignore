node_modules/
.DS_Store
.env
.env.*
*.log
*.tgz

# ClickUp OAuth app credentials — local only, never commit.
clickup-oauth.json

# Unrelated local scratch tooling — not part of this package.
analytics_debug_bridge.py
